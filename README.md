# Customer-Churn-using-XGBoost
This project builds a Customer Churn Prediction model using XGBoost. Data is cleaned, encoded, and split for training. Model performance is evaluated with accuracy, F1, and AUC. SHAP is used for global and local explainability, with summary and force plots saved as PNG for GitHub compatibility.
